class Singleton {
    private static Sigleton _instance {get; set;};

    public void InitSingleton() {
        if (_instance == null) _instance = new Singleton();
    }
}

public class SoundManger : Singleton { ... }

public class ManagerHolder {
    SoundMananger soundManager = new SoundManager();


    public OnStart() {
        soundManager.Init();
        gameManager.Init();
    }
}

publc class DungeonRoom {
    SoundManager soundManger;
    SoundManager.instance.Play("");

    void OnStart() {
        soundManger = SoundManager.instance;
    }
}